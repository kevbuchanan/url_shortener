<h1>Welcome to the Url shortener</h1>
 <%= erb :_form, layout: false %>
<h3>Existing Urls</h3>
<table>
  <thead>
    <th>Original</th>
    <th>Shortened</th>
  </thead>
  <tbody>
  <% @urls.each do |url| %>
    <tr>
      <td><%= url.original %></td>
      <td><%= url.shortened_url %></td>
    </tr>
  <% end %>
  </tbody>
</table>

